# 새 워크트리로 자동 복사할 로컬 파일 (.gitignore 문법).
# gitignore 대상이면서 아래 패턴과 매칭되는 파일만 복사된다 → 추적 파일은 중복되지 않는다.
# 이 두 파일이 없으면 새 워크트리의 첫 Gradle Sync가 SDK 경로·서명 설정 부재로 깨진다.
# 주의: 네이티브 `claude -w` 생성에만 적용된다. 수동 `git worktree add`에는 적용되지 않는다.
local.properties
keystore.properties
