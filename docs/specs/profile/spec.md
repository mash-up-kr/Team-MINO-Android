# 스펙 명세서: 프로필 설정 및 수정 (Profile Setup & Edit)

**대상 스펙 경로**: `docs/specs/profile`

**기준 PRD 버전**: 3.1.0

**최초 작성일**: 2026-08-16

**최종 수정일**: 2026-08-16

**상태**: CREATED

**버전**: 1.0.0

**입력**: 사용자 설명: "프로필 설정 및 수정에 해당하는 스펙을 정의한다. https://www.figma.com/design/5P3HE7q8MGc6yAr4rTOSZn/MU_%EB%94%94%EC%9E%90%EC%9D%B8?node-id=1728-114613&m=dev"

---

## 1. 유저 시나리오 (User Scenarios) *(필수)*
### 유저 플로우 1 - 온보딩에서 프로필을 처음 만든다
앱을 처음 설치한 사용자가 스플래시 직후 프로필 설정 화면에서 닉네임과 아바타를 정하고 저장한다. 저장이 끝나야 앱을 쓰기 시작할 수 있으므로 건너뛸 수 없다.

**Figma**: [010-1 프로필 설정_기본](https://www.figma.com/design/5P3HE7q8MGc6yAr4rTOSZn/MU_%EB%94%94%EC%9E%90%EC%9D%B8?node-id=2314-95662&m=dev) · [010-2 프로필 설정_입력 완료](https://www.figma.com/design/5P3HE7q8MGc6yAr4rTOSZn/MU_%EB%94%94%EC%9E%90%EC%9D%B8?node-id=2314-95709&m=dev)

**진입 조건**: 프로필이 없는 기기에서 [SCR-001] 스플래시가 끝나 프로필 설정 화면으로 자동 진입한 상태. 닉네임은 비어 있고, 상단 썸네일은 기본 아바타를 보여주며, `지우기`·`저장`은 모두 비활성이고 뒤로가기는 비활성이다.

1. 사용자가 `이름 또는 닉네임` 필드에 한글·영문 2글자 이상을 입력한다 → 시스템이 오류 표시 없이 값을 받고 `저장`을 활성 상태로 바꾼다
2. 사용자가 `프로필 이미지 선택` 목록에서 아바타 1개를 누른다 → 시스템이 그 아바타를 선택 상태로 표시하고 상단 썸네일을 즉시 그 아바타로 바꾼다. 이전에 고른 아바타가 있었다면 선택이 해제된다
3. 사용자가 `저장`을 누른다 → 시스템이 닉네임과 아바타를 사용자 프로필로 보관하고, 개인방(`내 장소`) 생성을 트리거한 뒤 온보딩의 공동방 생성 스텝으로 이동한다

**완료 조건**: 기기에 닉네임과 아바타를 가진 프로필이 1개 존재하고, 개인방(`내 장소`)이 생성되었으며, 화면이 온보딩의 공동방 생성 스텝으로 넘어가 있다.

**테스트 시나리오**

| ID | 시나리오 | Given | When | Then | 관련 FR |
|---|---|---|---|---|---|
| TS-001 | 진입 직후 저장 불가 | 프로필이 없는 기기에서 프로필 설정 화면에 막 진입했다 | 아무 입력도 하지 않는다 | `저장`과 `지우기`가 모두 비활성이고 뒤로가기가 비활성이다 | FR-004, FR-005, FR-010 |
| TS-002 | 닉네임만으로 저장 활성 | 프로필 설정 화면에 있고 아바타를 고르지 않았다 | 닉네임에 `민호`를 입력한다 | `저장`이 활성으로 바뀐다 | FR-002, FR-004 |
| TS-003 | 아바타 선택 즉시 썸네일 반영 | 닉네임 `민호`가 입력되어 있다 | 아바타 목록에서 3번째 아바타를 누른다 | 3번째 아바타만 선택 상태가 되고 상단 썸네일이 그 아바타로 바뀐다 | FR-003 |
| TS-004 | 아바타 단일 선택 | 3번째 아바타가 선택되어 있다 | 7번째 아바타를 누른다 | 7번째만 선택 상태가 되고 3번째는 선택 해제된다 | FR-003 |
| TS-005 | 온보딩 저장 후 이동 | 닉네임 `민호`, 아바타 1개가 선택되어 있다 | `저장`을 누른다 | 프로필이 보관되고 개인방(`내 장소`)이 생성되며 온보딩 공동방 생성 스텝으로 이동한다 | FR-007, FR-008 |
| TS-006 | 저장 실패 시 화면 유지 | 닉네임 `민호`, 5번째 아바타가 선택되어 있고 저장이 실패하는 상황이다 | `저장`을 누른다 | 프로필 설정 화면에 그대로 있고 닉네임 `민호`와 5번째 아바타 선택이 유지되며 실패가 사용자에게 알려진다 | FR-012 |

**엣지 케이스**

| ID | 상황 | 처리 | 관련 FR |
|---|---|---|---|
| EC-001 | 온보딩 도중 사용자가 시스템 뒤로가기(제스처·물리 키)를 시도한다 | 화면을 벗어나지 않고 프로필 설정 화면을 그대로 유지한다 | FR-010 |
| EC-002 | 아바타를 하나도 고르지 않은 채 `저장`을 누른다 | 저장을 막지 않고, 상단 썸네일에 보이던 기본 아바타를 프로필 아바타로 보관한다 | FR-003, FR-007 |
| EC-003 | `저장`을 눌렀으나 저장 요청이 실패한다 | 화면을 유지하고 입력한 닉네임·아바타 선택을 그대로 둔 채 실패를 알린다. 다음 화면으로 이동하지 않는다 | FR-012 |
| EC-004 | 저장 처리 중 사용자가 `저장`을 다시 누른다 | 추가 저장 요청을 만들지 않는다 | UX-003 |

---

### 유저 플로우 2 - 마이페이지에서 프로필을 수정한다
이미 프로필이 있는 사용자가 마이페이지에서 같은 화면을 열어 닉네임이나 아바타를 바꾼다. 온보딩과 화면 구성은 같고 진입·이탈 동작만 다르다.

**Figma**: [008-1 마이페이지](https://www.figma.com/design/5P3HE7q8MGc6yAr4rTOSZn/MU_%EB%94%94%EC%9E%90%EC%9D%B8?node-id=2542-124589&m=dev) · [010-2 프로필 설정_입력 완료](https://www.figma.com/design/5P3HE7q8MGc6yAr4rTOSZn/MU_%EB%94%94%EC%9E%90%EC%9D%B8?node-id=2314-95709&m=dev)

**진입 조건**: 프로필이 이미 있는 사용자가 [SCR-008] 마이페이지를 보고 있고, 프로필 영역에 현재 아바타와 닉네임이 표시되어 있다.

1. 사용자가 프로필 영역의 [연필]을 누른다 → 시스템이 프로필 설정 화면을 열고, 닉네임 필드와 아바타 선택 상태를 현재 저장된 값으로 채우며 뒤로가기를 활성으로 둔다
2. 사용자가 닉네임을 고치거나 다른 아바타를 고른다 → 시스템이 상단 썸네일과 `저장` 활성 상태를 입력에 맞춰 갱신한다
3. 사용자가 `저장`을 누른다 → 시스템이 바뀐 값을 프로필로 보관하고 마이페이지로 돌아가며, 마이페이지의 프로필 표기를 바뀐 값으로 보여준다

**완료 조건**: 저장된 프로필이 사용자가 입력한 닉네임·아바타와 같고, 화면이 마이페이지로 돌아와 바뀐 값을 표시하고 있다.

**테스트 시나리오**

| ID | 시나리오 | Given | When | Then | 관련 FR |
|---|---|---|---|---|---|
| TS-007 | 두 진입점의 화면 구성이 같다 | 프로필이 있는 사용자가 마이페이지를 보고 있다 | 프로필 영역의 [연필]을 눌러 프로필 설정 화면을 연다 | 제목 `프로필 설정`, 안내 문구 `친구들에게 어떻게 보일까요?`, 상단 썸네일, `이름 또는 닉네임` 필드, 아바타 12종 목록, `지우기`·`저장`이 온보딩 진입 때와 같은 구성으로 보인다 | FR-001 |
| TS-008 | 기존 값 프리필 | 닉네임 `민호`, 5번째 아바타로 저장된 프로필이 있다 | 마이페이지에서 [연필]을 누른다 | 닉네임 필드에 `민호`가 채워져 있고 5번째 아바타가 선택 상태이며 상단 썸네일이 그 아바타다 | FR-006 |
| TS-009 | 마이페이지 진입 시 뒤로가기 활성 | 마이페이지에서 프로필 설정 화면에 진입했다 | 뒤로가기를 누른다 | 저장하지 않고 마이페이지로 돌아가며 프로필은 진입 전 값 그대로다 | FR-010, FR-013 |
| TS-010 | 수정 저장 후 복귀 | 프로필 설정 화면에 진입해 닉네임을 `민호`에서 `민호짱`으로 고쳤다 | `저장`을 누른다 | 마이페이지로 돌아가고 마이페이지 프로필 표기가 `민호짱`이다 | FR-009 |
| TS-011 | 앱 전체 반영 | 닉네임을 `민호짱`으로 저장했다 | 내가 작성한 코멘트가 있는 장소 상세를 연다 | 코멘트 작성자 표기가 `민호짱`과 새 아바타로 보인다 | FR-007, FR-009 |

**엣지 케이스**

| ID | 상황 | 처리 | 관련 FR |
|---|---|---|---|
| EC-005 | 값을 고친 뒤 저장하지 않고 뒤로가기를 누른다 | 별도 확인 없이 화면을 닫고 수정 내용을 버린다. 저장된 프로필은 바뀌지 않는다 | FR-013 |
| EC-006 | 아무것도 바꾸지 않은 채 `저장`을 누른다 | 기존 값과 동일한 프로필을 보관한 것으로 처리하고 마이페이지로 돌아간다 | FR-009 |
| EC-007 | 수정 저장 요청이 실패한다 | 프로필 설정 화면을 유지하고 사용자가 고친 값을 그대로 둔 채 실패를 알린다. 마이페이지로 돌아가지 않는다 | FR-012 |

---

### 유저 플로우 3 - 닉네임 입력 오류를 고치고 입력을 초기화한다
사용자가 규칙에 맞지 않는 닉네임을 넣었을 때 화면이 이를 알려 주고, `지우기`로 입력을 한 번에 되돌린다.

**Figma**: [010-3 프로필 설정_입력 오류](https://www.figma.com/design/5P3HE7q8MGc6yAr4rTOSZn/MU_%EB%94%94%EC%9E%90%EC%9D%B8?node-id=2314-95754&m=dev) · [010-1 프로필 설정_기본](https://www.figma.com/design/5P3HE7q8MGc6yAr4rTOSZn/MU_%EB%94%94%EC%9E%90%EC%9D%B8?node-id=2314-95662&m=dev)

**진입 조건**: 프로필 설정 화면(진입점 무관)에서 사용자가 닉네임 필드에 입력하고 있는 상태.

1. 사용자가 규칙에 맞지 않는 닉네임(예: `민`, `abc1`)을 입력한다 → 시스템이 입력 필드를 오류 상태로 바꾸고 필드 아래에 `한글·영문 2글자 이상을 입력해주세요.`를 표시하며 `저장`을 비활성으로 유지한다
2. 사용자가 닉네임을 규칙에 맞게 고친다 → 시스템이 오류 표시를 걷어내고 `저장`을 활성으로 바꾼다
3. 사용자가 `지우기`를 누른다 → 시스템이 닉네임 입력과 아바타 선택을 모두 비운 상태로 되돌리고, 상단 썸네일을 기본 아바타로 되돌리며 `저장`을 비활성으로 바꾼다

**완료 조건**: 화면의 닉네임·아바타 입력 상태와 `저장` 활성 여부가 사용자가 마지막으로 입력한 값의 유효성과 일치한다.

**테스트 시나리오**

| ID | 시나리오 | Given | When | Then | 관련 FR |
|---|---|---|---|---|---|
| TS-012 | 1글자 닉네임 오류 | 프로필 설정 화면에 있다 | 닉네임에 `민`을 입력한다 | 입력 필드가 오류 상태가 되고 `한글·영문 2글자 이상을 입력해주세요.`가 필드 아래에 표시되며 `저장`이 비활성이다 | FR-002, FR-011, UX-001 |
| TS-013 | 허용되지 않는 문자 오류 | 프로필 설정 화면에 있다 | 닉네임에 `abc1`을 입력한다 | 입력 필드가 오류 상태가 되고 `저장`이 비활성이다 | FR-002, FR-011 |
| TS-014 | 오류 해소 | 닉네임이 `민`이라 오류 상태다 | 닉네임을 `민호`로 고친다 | 오류 표시가 사라지고 `저장`이 활성이 된다 | FR-002, FR-011 |
| TS-015 | 지우기 동작 | 닉네임 `민호`와 5번째 아바타가 선택되어 있다 | `지우기`를 누른다 | 닉네임이 비고, 어떤 아바타도 선택되지 않으며, 상단 썸네일이 기본 아바타로 돌아가고 `저장`·`지우기`가 모두 비활성이 된다 | FR-004, FR-005 |
| TS-016 | 아바타 없이는 지우기 비활성 | 아무 입력도 없는 상태다 | 닉네임에 `민호`만 입력한다 | `저장`은 활성이 되지만 `지우기`는 비활성으로 남는다 | FR-004, FR-005 |
| TS-017 | 긴 닉네임 허용 | 프로필 설정 화면에 있다 | 닉네임에 한글 30글자를 입력한다 | 오류 표시 없이 `저장`이 활성이다 | FR-002 |

**엣지 케이스**

| ID | 상황 | 처리 | 관련 FR |
|---|---|---|---|
| EC-008 | 닉네임 앞뒤에 공백만 붙여 입력한다(예: `  민호  `) | 앞뒤 공백을 제외한 값으로 길이를 판정하고, 저장 시에도 앞뒤 공백을 제외한 값을 보관한다 | FR-002, FR-007 |
| EC-009 | 닉네임에 공백만 입력한다 | 비어 있는 입력과 같이 다뤄 오류 상태로 두고 `저장`을 비활성으로 유지한다 | FR-002, FR-011 |
| EC-010 | 마이페이지에서 진입해 기존 닉네임을 모두 지운다 | 오류 상태로 바뀌고 `저장`이 비활성이 된다. 저장된 프로필은 그대로다 | FR-002, FR-011 |
| EC-011 | 비활성 상태의 `저장`이나 `지우기`를 누른다 | 아무 일도 일어나지 않는다. 별도 안내를 띄우지 않는다 | UX-004 |
| EC-012 | 아바타는 골랐지만 닉네임이 오류 상태다 | `저장`과 `지우기` 모두 비활성으로 둔다. 사용자는 닉네임을 직접 고쳐야 하며 `지우기`로 한 번에 되돌릴 수 없다 | FR-004, FR-005 |

## 2. 요구사항 (Requirements) *(필수)*
### 2.1 기능적 요구사항 (Functional Requirements)
- **FR-001**: 시스템은 반드시 온보딩 진입과 마이페이지 진입에 같은 프로필 설정 화면을 제공한다. 화면 제목은 `프로필 설정`, 안내 문구는 `친구들에게 어떻게 보일까요?`이며, 상단 프로필 썸네일 · `이름 또는 닉네임` 입력 필드 · `프로필 이미지 선택` 아바타 목록 · `지우기`/`저장` 버튼으로 구성된다. (근거: 디자인 정책 노드 `2314-95662`, PRD [SYS-011] "온보딩과 마이페이지가 같은 화면을 공유한다")
- **FR-002**: 시스템은 반드시 닉네임을 필수 입력으로 두고, 앞뒤 공백을 제외한 값이 **한글·영문으로만 이루어진 2글자 이상**일 때만 유효한 것으로 판정한다. **최대 길이 제한은 두지 않는다.** (근거: 디자인 정책 노드 `2314-95625` "한글·영문 2글자 이상 입력 필수", 플레이스홀더 `한글·영문 2글자 이상`)
- **FR-003**: 시스템은 반드시 앱이 제공하는 아바타 12종을 `프로필 이미지 선택` 목록에 노출하고, 그중 **한 번에 하나만** 선택되게 한다. 아바타를 누르면 즉시 그 아바타만 선택 상태가 되고 상단 프로필 썸네일이 같은 아바타로 바뀐다. 아바타 선택은 선택 입력이다. (근거: 디자인 정책 노드 `2314-95631` "하나만 선택 가능 / 선택하면 상단의 프로필 썸네일이 실시간 변경", 노드 `2314-95672` 아바타 3×4 그리드)
- **FR-004**: 시스템은 반드시 닉네임이 유효할 때만 `저장`을 활성 상태로 두고, 그 밖의 경우(비어 있음·오류 상태)에는 비활성으로 유지한다. 아바타 선택 여부는 `저장` 활성 조건에 영향을 주지 않는다. (근거: 디자인 정책 노드 `2314-95655` "'이름 또는 닉네임' 정상 입력 시 활성화")
- **FR-005**: 시스템은 반드시 **닉네임이 유효하고 아바타도 선택되어 있을 때만** `지우기`를 활성 상태로 두고, 눌렀을 때 닉네임 입력과 아바타 선택을 모두 초기 상태로 되돌리며 상단 썸네일을 기본 아바타로 되돌린다. (근거: 디자인 정책 노드 `2314-95649` "'이름 또는 닉네임' 정상 입력, 프로필 이미지 선택 시 활성화 / 클릭 시 1, 2 초기화")
- **FR-006**: 시스템은 반드시 마이페이지에서 진입했을 때 현재 저장된 닉네임과 아바타로 입력 상태를 미리 채운다. (근거: PRD [SCR-008] Flow A "프로필 영역의 [연필] 클릭 ➔ [SYS-011] 호출")
- **FR-007**: 시스템은 반드시 저장 시 닉네임(앞뒤 공백 제외)과 선택된 아바타를 기기 사용자의 **단일 공통 프로필**로 보관하고, 앱 안의 모든 프로필 표기 지점이 이 값을 사용하게 한다. (근거: PRD 비목표 "방별 프로필 분리 — [SYS-011]의 단일 공통 프로필을 모든 방에서 함께 사용한다")
- **FR-008**: 시스템은 반드시 온보딩 진입에서 저장이 완료되면 개인방(`내 장소`) 생성을 트리거한 뒤 온보딩의 공동방 생성 스텝으로 이동시킨다. (근거: 디자인 정책 노드 `2314-95655` "최초 접속 시 : 클릭 시 001로 이동", PRD [SCR-002] Flow A)
- **FR-009**: 시스템은 반드시 마이페이지 진입에서 저장이 완료되면 마이페이지로 돌아가고, 마이페이지의 프로필 표기를 저장한 값으로 즉시 갱신한다. (근거: 디자인 정책 노드 `2314-95655` "008[마이페이지]에서 접속 시 : 008[마이페이지]로 이동", PRD [SCR-008] Flow A)
- **FR-010**: 시스템은 반드시 온보딩 진입에서는 뒤로가기를 비활성으로 두어 화면을 벗어나지 못하게 하고, 마이페이지 진입에서는 뒤로가기를 활성으로 둔다. (근거: 디자인 정책 노드 `2314-95643` "최초 접속 시 : 비활성화 / 008[마이페이지]에서 접속 시 : 활성화")
- **FR-011**: 시스템은 반드시 닉네임이 유효하지 않은 동안 입력 필드를 오류 상태로 표시하고 필드 아래에 `한글·영문 2글자 이상을 입력해주세요.`를 보여 준다. 값이 유효해지면 오류 표시를 걷어낸다. (근거: 디자인 정책 노드 `2314-95754` 010-3 프로필 설정_입력 오류)
- **FR-012**: 시스템은 반드시 저장이 실패하면 프로필 설정 화면과 사용자가 입력한 값을 그대로 유지한 채 실패를 알리고, 후속 화면으로 이동하지 않는다. (근거: PRD [SYS-011] Flow C — 저장 성공을 전제로만 후속 분기가 정의됨)
- **FR-013**: 시스템은 반드시 마이페이지 진입에서 뒤로가기를 누르면 저장하지 않은 수정 내용을 버리고 마이페이지로 돌아간다. (근거: 디자인 정책 노드 `2314-95643` — 확인 모달이 설계되어 있지 않음)

### 2.2 핵심 UX 규칙 (Core UX Rules)
- **UX-001**: 닉네임 오류는 별도 팝업·토스트·상단 배너가 아니라 입력 필드 바로 아래 안내 문구 자리에 표시한다. 오류가 없을 때 같은 자리에는 회색 안내 문구가 놓인다. (근거: 디자인 정책 노드 `2314-95754` / `2314-95662`)
- **UX-002**: 닉네임 유효성 판정과 `저장` 활성 상태 변화는 사용자가 글자를 입력·삭제할 때마다 반영하며, 별도 확인 조작(포커스 이동·버튼 클릭)을 요구하지 않는다. (근거: 디자인 정책 노드 `2314-95655`)
- **UX-003**: 저장 요청이 끝나기 전에는 같은 요청이 다시 만들어지지 않게 하고, 처리 중에는 `저장`을 다시 누를 수 없게 한다.
- **UX-004**: 비활성 상태의 `저장`·`지우기`는 눌러도 아무 반응을 내지 않는다. 비활성 이유를 별도 안내로 띄우지 않는다. (근거: 디자인 정책 노드 `2314-95662` — 비활성 상태에 안내 요소가 없음)
- **UX-005**: 아바타를 고르면 상단 프로필 썸네일이 같은 화면 안에서 즉시 바뀐다. 미리보기를 위한 별도 화면 이동이나 확인 단계를 두지 않는다. (근거: 디자인 정책 노드 `2314-95631`)
- **UX-006**: 프로필 설정 화면에서는 바텀 네비게이션을 노출하지 않는다. (근거: PRD [SCR-002] 화면 특성, 디자인 정책 노드 `2314-95662` — 하단이 Action Area로 채워짐)

### 2.3 주요 도메인 (Core Domain) *(데이터를 다루는 기능일 경우 포함)*

- **프로필 (Profile)**: 기기 사용자가 다른 사람에게 보이는 이름과 얼굴. 닉네임과 아바타 하나로 이루어지며, 한 기기에 하나만 존재한다. 방·장소·코멘트 어디에서든 같은 프로필이 쓰인다.
- **아바타 (Avatar)**: 앱이 제공하는 고정 12종 캐릭터 이미지 중 하나. 사용자가 직접 이미지를 올리지 않으며, 프로필은 이 목록 중 하나를 가리킨다.
- **개인방 (`내 장소`)**: 프로필이 처음 저장될 때 함께 생기는 사용자 전용 저장 공간. 이 스펙은 프로필 저장이 개인방 생성을 촉발한다는 사실만 다루고, 개인방의 구성과 동작은 방 스펙이 정의한다.

---

## 3. 범위 (Scope) *(필수)*
### 3.1 측정 가능한 성과 (Measurable Performance)
- **SC-001**: 앱을 처음 켠 사용자가 프로필 설정 화면에 진입한 뒤 **60초 이내**에 저장을 마치고 다음 온보딩 스텝으로 넘어간다.
- **SC-002**: 온보딩에 진입한 사용자의 **95% 이상**이 이 화면을 이탈하지 않고 프로필 저장까지 도달한다.
- **SC-003**: 닉네임을 고쳐 저장한 뒤, 앱 안의 모든 프로필 표기 지점(마이페이지 헤더·코멘트 작성자·방 멤버 아바타)이 **앱을 다시 켜지 않고도** 바뀐 값을 보여 준다.
- **SC-004**: 유효하지 않은 닉네임(2글자 미만, 한글·영문 이외 문자 포함)으로 저장이 시도되는 경우가 **0건**이다.
- **SC-005**: 아바타를 누른 시점부터 상단 프로필 썸네일이 바뀌기까지 사용자가 추가로 하는 조작이 **0회**다.
- **SC-006**: 프로필 저장이 실패한 경우, 사용자가 화면을 다시 채워 넣지 않고 **입력한 값 그대로** 재시도할 수 있다.

### 3.2 비목표 항목 (Out of Scope)
- 개인방(`내 장소`)의 생성 규칙·표시·삭제 제약은 방 스펙이 정의한다. 이 문서는 프로필 저장이 개인방 생성을 촉발한다는 사실까지만 다룬다.
- 온보딩의 나머지 스텝(공동방 생성 · 친구 초대 · 공유 방법 튜토리얼)과 스텝 간 진행 규칙은 온보딩 스펙이 정의한다.
- 마이페이지의 프로필 영역을 뺀 나머지 항목(다크모드 · 알림 설정 · 위치 설정 · 약관 및 동의 · 앱 리뷰)은 마이페이지 스펙이 정의한다.
- 아바타 12종의 이미지 에셋과 시각 규격(크기·간격·선택 표시 스타일)은 디자인 시스템이 정의한다.
- 프로필이 다른 화면에 어떻게 그려지는지(코멘트 작성자 표기, 방 멤버 아바타 겹침·카운터 규칙)는 각 화면의 스펙이 정의한다.
- 닉네임 금칙어·비속어 필터, 닉네임 중복 제한 같은 내용 정책은 서버가 정의한다.
- 프로필 이미지 직접 업로드·촬영, 방마다 다른 프로필을 두는 것은 MVP 비목표이므로 이 문서가 다루지 않는다.
- 기기 식별자 발급과 세션 유지 방식은 디바이스 세션 스펙이 정의한다.
- 저장 실패를 사용자에게 알리는 표현의 구체적 형태는 프로젝트 공통 에러 처리 규약을 따르며 이 문서가 따로 정의하지 않는다.

---

## 4. 가정 (Assumptions)
- 프로필은 회원가입 없이 앱이 발급한 기기 식별자에 귀속된다. 프로필을 만들 수 있는 세션은 이 화면에 오기 전에 이미 확보되어 있다. (PRD 핵심 원칙 "비회원 디바이스 세션")
- 닉네임 중복은 허용한다. 디자인에 중복 검사 결과를 알리는 상태가 없으므로 유일성을 요구하지 않는 것으로 본다.
- 닉네임의 `한글·영문`은 한글 음절과 영문 알파벳만을 뜻하며, 숫자·특수문자·이모지·공백은 유효하지 않은 문자로 본다. 공동방 이름이 숫자를 허용하는 것과 달리 디자인 문구에 숫자가 빠져 있는 것을 그대로 따른다.
- 닉네임의 앞뒤 공백은 길이 판정과 보관 양쪽에서 제외한다.
- 아바타는 앱에 포함된 고정 12종이며, 목록을 서버에서 내려받지 않는다. (디자인 정책 노드 `2314-95672`의 3×4 그리드)
- 아바타를 고르지 않고 저장하면 상단 썸네일에 처음부터 보이던 기본 아바타가 프로필 아바타로 보관된다. `프로필 이미지 선택`에는 필수 표시가 없고 `저장` 활성 조건에도 들어가지 않으므로 선택 입력으로 본다.
- 마이페이지에서 수정 중 뒤로가기를 누를 때 확인 모달을 띄우지 않는다. 디자인에 해당 모달이 없다.
- 프로필 저장은 서버 반영을 포함하며, 사용자는 저장 시점에 네트워크에 연결되어 있다고 가정한다. 오프라인 저장·나중에 동기화는 다루지 않는다.
- 저장 실패의 사용자 안내는 프로젝트 공통 에러 처리 규약(`docs/conventions/error_handling.md`)이 제공하는 표현을 그대로 쓴다.
- 온보딩의 프로필 설정 스텝에는 `건너뛰기`가 없다. 디자인 정책 노드 `2314-95611`의 주석 1~5에 건너뛰기 항목이 없고, PRD [SCR-002] Flow A가 "건너뛰기 불가"로 못박고 있다.
- 닉네임 길이에 상한이 없으므로, 좁은 자리에 긴 닉네임이 들어갈 때의 말줄임 처리는 그 화면을 정의하는 스펙이 책임진다.

---

## 5. TBD

### 확정된 항목

- **Q**: 닉네임의 최대 글자 수는 몇 자인가? — 디자인 정책 노드 `2314-95625`와 플레이스홀더 모두 하한(한글·영문 2글자 이상)만 말하고 상한을 말하지 않는다.
  → **A**: **상한을 두지 않는다.** 검증은 하한만 하고, 긴 닉네임이 좁은 자리에 들어갈 때의 말줄임은 표시하는 화면이 처리한다. (FR-002 반영)
- **Q**: `지우기` 버튼의 활성 조건은 무엇인가? — 디자인 정책 노드 `2314-95649`에 "'이름 또는 닉네임' 정상 입력, 프로필 이미지 선택 시 활성화"와 "'이름 또는 닉네임' 정상 입력 시 활성화"가 나란히 적혀 충돌한다.
  → **A**: **닉네임이 유효하고 아바타도 선택되어 있을 때만 활성화**한다. 주석 첫 줄을 따르며, 아바타를 고르지 않았거나 닉네임이 오류 상태이면 비활성으로 둔다. (FR-005, EC-012 반영)
