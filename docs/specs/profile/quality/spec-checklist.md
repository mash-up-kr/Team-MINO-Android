# Spec 품질 체크리스트: 프로필 설정 및 수정 (Profile Setup & Edit)

**목적**: Plan 단계로 넘어가기 전 Spec의 완성도와 품질을 검증한다

**작성일**: 2026-08-31

**대상 스펙**: [spec.md](../spec.md) - 4.0.0

**상태** : PASS

---

## 스펙 품질 (Spec Quality)

- [x] 구현 세부사항(언어, 프레임워크, API)이 없다
- [x] 사용자 가치와 비즈니스 요구에 초점이 맞춰져 있다
- [x] 비기술 이해관계자(기획자, 디자이너 등)가 읽을 수 있게 작성되었다
- [x] 필수 섹션이 모두 채워져 있다

## 요구사항 완전성 (Requirement Completeness)

- [x] `[TBD]` 마커가 남아 있지 않다
- [x] 요구사항이 검증 가능하고 모호하지 않다
- [x] 테스트 시나리오 및 완료조건이 모두 정의되어 있다
- [x] 엣지 케이스가 식별되어 있다
- [x] 핵심 UX 규칙이 측정 가능하다
- [x] 핵심 UX 규칙이 기술 중립적이다 (구현 세부사항 없음)
- [x] 범위가 명확히 한정되어 있다
- [x] 의존성과 가정이 식별되어 있다

## 스펙 완성도 (Spec Completeness)

- [x] 모든 기능 요구사항에 해당하는 명확한 테스트 시나리오가 있다
- [x] 유저 시나리오가 주요 플로우를 포괄한다
- [x] 기능이 성공 기준에 정의된 측정 가능한 결과를 충족한다
- [x] 구현 세부사항이 명세로 새어 들어오지 않았다

## 발견된 문제

### 6차 검증 (2026-08-31, spec 4.0.0)

`[TBD]` 마커 **0건**, 실패 항목 **없음**. 전 16항목 통과.

**이번 개정에서 대조한 것**

- 기준 PRD는 **10.0.0 그대로**다. 5차 검증 이후 `docs/prd/business-context.md`가 개정되지 않아 PRD발 변경은 없다. 이번 개정의 입력은 PRD가 아니라 **서버 닉네임 정책표**와 **Figma `[010] 프로필 설정` 재조회**다.
- **서버 닉네임 정책표는 spec 3.0.0과 이미 일치했다.** 여섯 항목을 하나씩 대조했다 — 길이 `2~15자`(trim 후) = FR-002+FR-014 / 허용 문자 `완성형 한글 + 영문 대소문자만` = FR-002 / 불허 `숫자·특수문자·이모지·공백(내부 포함)·자모 단독` = FR-002 / 앞뒤 공백 `거절이 아니라 trim 후 검증·저장` = EC-008 / 중복 `허용` = §4 가정 / 필수 여부 `등록 시 둘 다 필수, 수정 시 각각 optional` = 앱이 언제나 두 값을 함께 보내 충족. **요구사항 변경은 없고, 3.0.0 문면이 읽는 사람에게 맡겨 두었던 두 가지(자모 단독·내부 공백)를 FR-002 본문에 명시하는 데서 그쳤다.**
- 서버 정책표의 통과·거절 예시도 이 spec의 판정과 모두 같다 — 통과 `꾹이`·`GgukLover`·`ab`·`가`×15, 거절 `가`·`가`×16·`꾹이!`·`꾹 러버`·`gguk_1`·`꾹이2`·`😀꾹`. 이 중 `가`×16만 FR-014의 입력 차단이 서버에 닿기 전에 막고, 나머지 거절은 FR-002가 판정한다.
- **Figma 재조회가 어긋남 3건을 드러냈다** — 이번 개정의 실질은 여기다. 아래 표에 정리했다.
- 아바타 브랜딩 에셋 교체(12종 캐릭터)는 목록 구성(3×4 그리드·12칸)을 바꾸지 않아 요구사항에 닿지 않는다. 에셋과 시각 규격은 §3.2 비목표대로 디자인 시스템의 몫이다.

**이번 개정에서 닫은 논의 4건** (모두 사용자 확정)

| # | 논의 | 결정 | 반영 |
|---|---|---|---|
| 1 | 기본 아바타(미선택 시 상단 썸네일)의 정체 — 디자인 노드 `2314-95665`의 기본 썸네일이 목록 첫 항목(노드 `2314-95674`)과 배경색도 생김새도 다르고, 서버 아바타 색이 12색 + 1의 13개다 | **선택 목록 12종 밖의 13번째 아바타다.** 고르지 않고 저장하면 이 값이 그대로 나가며 12종 중 하나로 바꿔치지 않는다 | **FR-015 신설**, FR-003 본문 변경, **EC-002 재정의**, **TS-022·TS-023 신설**, §2.3 도메인·§3.2 비목표·§4 가정 3줄 |
| 2 | 필드 아래 문구를 하나로 둘 것인가 사유별로 가를 것인가 — 디자인에는 `최대 15자까지 입력할 수 있어요.` 한 줄만 있고 오류일 때 색만 바뀐다 | **사유별로 가른다.** 평상시는 디자인대로, 오류 시에는 같은 자리를 `한글·영문 2글자 이상을 입력해주세요.`로 바꾼다. 자리는 하나뿐이라 화면 구조는 디자인과 같다 | **FR-011 본문 변경**, **UX-007 본문 변경(3.0.0 확정 번복)**, UX-001 보강, **TS-025 신설**, TS-021 갱신, 유저 플로우 3 스텝 1·2 |
| 3 | 3.0.0의 15자 입력 차단을 유지하는가 — 디자인 `010-3`이 상한을 말하는 문구를 오류 색으로 띄운다 | **입력 차단 유지.** `010-3`의 샘플 값 `민호야민호야민호호로호호호호`는 **14자 완성형 한글**이라 하한·상한·문자 규칙 어디에도 걸리지 않는다 — 상한 초과를 그린 화면이 아니라 오류 상태의 생김새를 보여 주는 예시로 읽는다 | FR-014·TS-017·TS-020 **유지**(변경 없음) |
| 4 | 서버 정책과 남은 어긋남이 있는가 | **없다.** 여섯 항목 모두 일치한다. 다만 자모 단독·내부 공백을 요구사항 본문에 명시한다 | FR-002 본문 변경, SC-004 열거 갱신, **TS-024 신설**, §4 가정 2줄 교체 |

1번이 EC-002의 처리를 바꾸고(기본 아바타를 12종 중 하나로 보지 않게 됨) 2번이 3.0.0에서 신설한 UX-007을 정반대로 뒤집으므로 등급은 **MAJOR(3.0.0 → 4.0.0)** 다.

**이번 개정에는 코드 재작업이 따른다**

- `ProfileAvatar`(`:core:domain`)에 기본 아바타 값이 없다 — `Default = Person1`로 목록 첫 항목을 빌려 쓰고 있어 FR-015와 어긋난다.
- `ProfileMapper`(`:core:data`)의 색 표가 13번째 색을 **"방에서 색을 고르지 않음"** 으로 해석해 프로필에서 배제하고 있다("`gray`를 내보내는 경로는 없다"). 1번 확정이 이 해석을 뒤집는다.
- `:core:design-system`에 기본 아바타 에셋이 없다(현재 12종만 있다).
- 오류 문구 `한글·영문 2글자 이상을 입력해주세요.`가 `feature/profile`의 `strings.xml`에 없다 — 화면이 평상시 문구 하나만 쓰고 있어 2번 확정을 만족하지 않는다.
- 상한 입력 차단(FR-014)은 3.0.0에서 확정된 뒤 아직 구현되지 않았다(5차 검증 기록 그대로 유지).

**PRD로 되돌려야 할 어긋남 (2건 유지 · 1건 신규)**

- **(유지, 4차에서 보고)** PRD `[SYS-011]` Flow D("앱 최초 접속 시에는 비활성")가 spec FR-010("노출하지 않는다")과 어긋난다. 같은 PRD의 `[SYS-001]`은 "표출하지 않는다"로 적는다. **10.0.0에서도 고쳐지지 않았다.**
- **(유지, 3차에서 보고)** PRD `[SYS-011]` Flow A가 인용한 `프로필 이미지를 꾸며보세요.`는 디자인 `010-1`의 숨김 구버전 프레임(노드 `2314:95690`) 텍스트다. 실제 노출 문구는 `프로필 이미지 선택`(노드 `2314:95671`)이다. **10.0.0에서도 고쳐지지 않았다.**
- **(신규)** PRD `[SYS-011]` Flow A가 아바타를 "제공 아바타 목록에서 1개 선택"으로만 적어 **기본 아바타의 존재를 말하지 않는다.** 1번 확정으로 기본 아바타가 목록 밖의 어엿한 값이 됐으므로 PRD에도 반영되는 것이 맞다.

**FR ↔ 테스트 시나리오 대응 (재확인, 15/15 커버)**

FR-001 → TS-007 / FR-002 → TS-002·012·013·014·017·**024**, EC-008·009·010·014 / FR-003 → TS-003·004·019 / FR-004 → TS-001·002·015·016, EC-012 / FR-005 → TS-001·015·016, EC-012 / FR-006 → TS-008 / FR-007 → TS-005·011·**023**, EC-002·008 / FR-008 → TS-005·018, EC-013 / FR-009 → TS-010·011, EC-006·013 / FR-010 → TS-001·009, EC-001 / FR-011 → TS-012·013·014·**024**·**025**, EC-009·010 / FR-012 → TS-006, EC-003·007·014 / FR-013 → TS-009, EC-005 / FR-014 → TS-017·020·021, EC-014 / **FR-015 → TS-022·023, EC-002**. 빠진 FR 없음.

UX 규칙 대응: UX-001 → TS-012·**025** / UX-002 → TS-014 / UX-003 → EC-004 / UX-004 → EC-011 / UX-005 → TS-003 / UX-006 → (화면 구성, 시각 검증) / UX-007 → TS-021.

**SC 갱신**

- SC-004의 열거를 서버 정책표에 맞춰 `2글자 미만, 15자 초과, 자모 단독, 공백·숫자·특수문자·이모지 포함`으로 넓히고, "서버가 닉네임 형식을 이유로 저장을 거절하는 일이 없다"를 성과로 덧붙였다. 여섯 항목이 모두 일치하므로 이제 이 0건이 실제로 달성 가능한 목표다.

### 5차 검증 (2026-08-31, spec 3.0.0)

`[TBD]` 마커 **0건**, 실패 항목 **없음**. 전 16항목 통과.

**이번 개정에서 대조한 것**

- 기준 PRD 9.0.0 → 10.0.0 재대조. **프로필에 닿는 변경은 1건**이다 — `[SYS-011]` Flow A의 닉네임 규칙이 `한글·영문 2글자 이상` → **`한글·영문 2글자 이상 15자 이하`** 로 바뀌었다(근거: 디자인 정책 `[005]` 주석 #3 `닉네임` 최대 15자).
- 10.0.0의 나머지 변경 — 홈 카드 `다른 방 저장`이 「방 선택 시트」를 쓰도록 재정의(`[SCR-003]` Flow C), 「홈 방 시트」의 방 변경 전용화, `[SCR-006]` 장소 상세 헤더의 장소분류 라벨 → 등록자 닉네임 교체, `[SYS-003]` 시트 높이 2단 3케이스, 핀 마커 장소명·코멘트 작성 시각·캐릭터/툴팁 배치 수치 — 는 모두 이 spec의 요구사항 밖이다.
- 다만 `[SCR-006]` 헤더가 등록자 닉네임을 노출하게 되어 **프로필 표기 지점이 하나 늘었다.** SC-003의 열거에 반영했다(요구사항 변경 아님, 성과 기준의 대상 범위 보정).
- `[SCR-001]`·`[SCR-002]`·`[SCR-008]` 세 섹션은 9.0.0과 동일했다.

**이번 개정에서 닫은 논의 2건** (모두 사용자 확정)

| # | 논의 | 결정 | 반영 |
|---|---|---|---|
| 1 | PRD 10.0.0의 15자 상한 vs spec 2.0.0의 "상한을 두지 않는다" 확정 | **PRD를 따라 15자 상한을 채택한다(2.0.0 확정 번복).** 서버 스키마 `maxLength: 15`와의 어긋남도 함께 사라진다 | **FR-002 본문 변경**, **FR-014 신설**, TS-017 반전, **TS-020 신설**, EC-014 재정의, SC-003·SC-004, §4 가정 3줄 |
| 2 | 16번째 글자를 어떻게 다루는가 | **입력 차단.** 오류 상태를 만들지 않으므로 디자인에 없는 오류 문구를 신설할 필요가 없다 — 2.0.0이 상한을 거부했던 근거가 이 방식에서는 성립하지 않는다. 카운터도 두지 않는다 | FR-014, **UX-007 신설**, **TS-021 신설**, 유저 플로우 3 스텝 3 |

1번이 확정 FR(FR-002)의 경계를 바꾸고 TS-017을 반대로 뒤집으므로 등급은 **MAJOR(2.0.0 → 3.0.0)** 다. **4차 개정과 달리 이번에는 코드 재작업이 따른다** — `ValidateNicknameUseCase`의 KDoc과 구현이 "길이 상한은 두지 않는다"를 전제하고 있고, 입력 차단은 아직 어디에도 없다.

**PRD로 되돌려야 할 어긋남 (2건 유지 · 1건 신규 관찰)**

- **(유지, 4차에서 보고)** PRD `[SYS-011]` Flow D("앱 최초 접속 시에는 비활성")가 spec FR-010("노출하지 않는다")과 어긋난다. 같은 PRD의 `[SYS-001]`은 "표출하지 않는다"로 적고 있어 PRD 내부에서도 두 표현이 갈린다. **10.0.0에서도 고쳐지지 않았다.**
- **(유지, 3차에서 보고)** PRD `[SYS-011]` Flow A가 인용한 `프로필 이미지를 꾸며보세요.`는 디자인 `010-1`의 숨김 구버전 프레임(노드 `2314:95690`) 텍스트다. 실제 노출 문구는 `프로필 이미지 선택`(노드 `2314:95671`)이고 spec FR-001·TS-007이 후자를 쓴다. **10.0.0에서도 고쳐지지 않았다.**
- **(신규 관찰, spec 수정 불필요)** 15자 상한의 출처인 주석 #3은 `[005]` 장소 상세 & 코멘트 보드에 있고, PRD가 이를 `[SYS-011]` Flow A(프로필 **입력** 규칙)로 라우팅했다. 같은 주석을 장소 상세 헤더의 닉네임 **표시** 상한으로 읽을 여지도 있으나, PRD의 라우팅을 원본으로 받아들이기로 사용자가 확정했다(§5 3.0.0). 표시 쪽 말줄임은 §4 가정대로 그 화면 스펙의 몫이다.

**FR ↔ 테스트 시나리오 대응 (재확인, 14/14 커버)**

FR-001 → TS-007 / FR-002 → TS-002·012·013·014·**017**, EC-008·009·010·014 / FR-003 → TS-003·004·019, EC-002 / FR-004 → TS-001·002·015·016, EC-012 / FR-005 → TS-001·015·016, EC-012 / FR-006 → TS-008 / FR-007 → TS-005·011, EC-002·008 / FR-008 → TS-005·018, EC-013 / FR-009 → TS-010·011, EC-006·013 / FR-010 → TS-001·009, EC-001 / FR-011 → TS-012·013·014, EC-009·010 / FR-012 → TS-006, EC-003·007·014 / FR-013 → TS-009, EC-005 / **FR-014 → TS-017·020, EC-014**. 빠진 FR 없음.

UX 규칙 대응: UX-001 → TS-012 / UX-002 → TS-014 / UX-003 → EC-004 / UX-004 → EC-011 / UX-005 → TS-003 / UX-006 → (화면 구성, 시각 검증) / **UX-007 → TS-021**.

**SC-004와 1번 결정의 관계 (4차 기록 갱신)**

- 4차는 "상한이 SC-004의 열거에 없으므로 16자 이상 저장 시도는 SC-004 위반이 아니다"로 정리했다. 3.0.0에서 상한을 채택했으므로 **SC-004의 열거에 `15자 초과`를 넣었다.** FR-014의 입력 차단이 그 0건을 보장한다.

### 4차 검증 (2026-08-28, spec 2.0.0)

`[TBD]` 마커 **0건**, 실패 항목 **없음**. 전 16항목 통과.

**이번 개정에서 대조한 것**

- 기준 PRD 5.0.0 → 9.0.0 재대조. **프로필에 닿는 변경은 하나도 없다.** `[SYS-011]`·`[SCR-001]`·`[SCR-002]`·`[SCR-008]` 네 섹션을 5.0.0 시점(`8ab1d05`)과 기계 대조한 결과 **네 섹션 모두 완전히 동일**했다. 5.1.0~9.0.0의 실제 변경은 `[SCR-003]` 홈 카드 덱(전환 모델·소진 예고 툴팁·장소 확인 이벤트 분리)·`[SCR-006]` 장소 상세(저장된 방 시트)와 그에 딸린 용어·§5 확정 항목, 그리고 §2 비목표 3줄(`장소 가리기`·`[장소 더 보기]` 스펙아웃)이다.
- 따라서 이번 개정의 요구사항 변경은 PRD가 아니라 **plan 4.0.0·tasks가 드러낸 미해결 4건**을 §5에서 확정한 결과다. 기준 PRD 버전만 9.0.0으로 맞췄다.

**이번 개정에서 닫은 논의 4건** (모두 사용자 확정)

| # | 논의 | 결정 | 반영 |
|---|---|---|---|
| 1 | 서버가 닉네임을 15자로 제한한다. §5의 "상한 없음" 확정을 뒤집는가? | **상한을 두지 않는다(유지).** 16자 이상은 서버가 거절해 저장 실패로 보인다 | FR-002·TS-017 유지, **EC-014 신설** |
| 2 | 서버는 닉네임에 공백을 허용한다. §4·§5의 "공백 불가"를 뒤집는가? | **공백 불가(유지).** 클라이언트가 더 좁아 실패가 나지 않는다 | FR-002 유지, §4 가정 보강 |
| 3 | FR-010의 "노출하되 비활성" vs 구현의 "숨김" | **노출하지 않는다** | **FR-010 본문 변경**, TS-001, 유저 플로우 1 진입 조건 |
| 4 | FR-003의 칸 단위 선택 표시가 원본에 없다 | **칸 단위 표시 요구를 걷어낸다.** 선택은 상단 썸네일이 드러내고, 보조 수단에는 전달한다 | **FR-003 본문 변경**, TS-003·TS-004, **TS-019 신설** |

3·4번이 확정 FR의 의미를 바꾸므로 등급은 MAJOR다. **다만 코드 재작업은 없다** — 구현을 spec에 맞추는 것이 아니라 이미 확정·구현된 쪽으로 spec을 정정하는 방향이기 때문이다.

**PRD로 되돌려야 할 어긋남 1건 (신규)**

- 3번 확정의 결과로 **PRD [SYS-011] Flow D("앱 최초 접속 시에는 비활성")가 이 spec과 어긋나게 됐다.** 같은 PRD의 [SYS-001]은 온보딩에서 뒤로가기를 "표출하지 않는다"로 적고 있어 PRD 내부에서도 두 표현이 갈린다. **PRD 개정이 뒤따라야 한다.**

**PRD ↔ 디자인 어긋남 1건 (3차에서 보고, 그대로 유지)**

- PRD [SYS-011] Flow A가 인용한 `프로필 이미지를 꾸며보세요.`는 디자인 `010-1`의 숨김 구버전 프레임(노드 `2314:95690`) 텍스트다. 실제 노출 문구는 `프로필 이미지 선택`(노드 `2314:95671`)이고 spec FR-001·TS-007이 후자를 쓴다. PRD 쪽 인용을 바로잡는 것이 맞다. 9.0.0에서도 고쳐지지 않았다.

**SC-004와 1번 결정의 관계 (확인)**

- SC-004는 유효하지 않은 닉네임을 "2글자 미만, 한글·영문 이외 문자 포함"으로 열거한다. **상한은 그 열거에 없으므로** 16자 이상 닉네임이 저장 시도되는 것은 SC-004 위반이 아니다. 두 항목이 충돌하지 않는다.

**FR ↔ 테스트 시나리오 대응 (재확인, 13/13 커버)**

FR-001 → TS-007 / FR-002 → TS-002·012·013·014·017, EC-008·009·010·**014** / FR-003 → TS-003·004·**019**, EC-002 / FR-004 → TS-001·002·015·016, EC-012 / FR-005 → TS-001·015·016, EC-012 / FR-006 → TS-008 / FR-007 → TS-005·011, EC-002·008 / FR-008 → TS-005·018, EC-013 / FR-009 → TS-010·011, EC-006·013 / FR-010 → TS-001·009, EC-001 / FR-011 → TS-012·013·014, EC-009·010 / FR-012 → TS-006, EC-003·007·**014** / FR-013 → TS-009, EC-005. 빠진 FR 없음.

### 3차 검증 (2026-08-21, spec 1.3.0)

`[TBD]` 마커 **0건**, 실패 항목 **없음**. 전 16항목 통과.

**이번 개정에서 대조한 것**

- 기준 PRD 4.2.0 → 5.0.0 재대조. [SYS-011] 프로필 설정 본문은 **한 글자도 바뀌지 않았고**, 디자인 정책 `[010] 프로필 설정` 주석(노드 `2314-95611`의 주석 1~5)도 그대로다. PRD 5.0.0의 "디자인 정책 개정 반영"은 `[000]` 튜토리얼 · `[001]` 공동방 생성 · `[012]` 스플래시 쪽 변경이다.
- 프로필에 닿는 PRD 변경은 3건 — ① 익명 세션이 **설치가 살아 있는 동안 만료되지 않음** 확정, ② [SYS-001] 온보딩 공동방 생성 단계의 **뒤로가기 미표출**(프로필 설정 복귀 경로 삭제), ③ [SCR-001] 스플래시 실패 정책이 Flow E로 확정(상태 4종).
- ①은 §4 가정과 §5의 세션 무효화 항목, ③은 §4 가정과 §5의 세션 확보 실패 항목의 근거를 갱신하는 데서 끝났다. ②만 요구사항 본문에 닿아 FR-008에 "되돌아오는 경로를 두지 않는다"를 추가하고 TS-018 · EC-013을 신설했다.
- **FR-001~FR-013 · UX-001~UX-006의 판정 내용은 하나도 바뀌지 않았다.** FR-008은 기존 분기(개인방 생성 → 공동방 생성 스텝)를 그대로 두고 복귀 경로 부재만 덧붙였다.

**PRD ↔ 디자인 어긋남 1건 (spec 수정 불필요)**

- PRD [SYS-011] Flow A가 아바타 목록 안내 문구로 인용한 `프로필 이미지를 꾸며보세요.`는 디자인 `010-1`의 **숨김(hidden) 구버전 프레임**(노드 `2314:95690`) 텍스트다. 실제 노출 문구는 `프로필 이미지 선택`(노드 `2314:95671`)이며 spec FR-001 · TS-007이 이미 후자를 쓰고 있다. PRD 쪽 인용을 바로잡는 것이 맞다.

**이번 개정에서 닫은 논의 3건**

- 온보딩 저장 후 프로필 설정 복귀 → **불가**. 값을 고치려면 온보딩을 끝내고 마이페이지에서 수정한다 (FR-008 · TS-018 · EC-013)
- 초대 링크로 들어온 프로필 미생성 사용자의 저장 후 분기 → **온보딩을 그대로 태운다**. 별도 분기를 두지 않고 방 참여는 온보딩 종료 후 초대 링크 스펙이 처리한다 (§4 가정, §3.2 비목표)
- 프로필 저장 후 온보딩 미완료 상태에서의 앱 재실행 → **이 스펙 범위 밖**. 스플래시·온보딩 스펙이 정한다 (§3.2 비목표)

**FR ↔ 테스트 시나리오 대응 (재확인)**

FR-001 → TS-007 / FR-002 → TS-002·012·013·014·017 / FR-003 → TS-003·004 / FR-004 → TS-001·002·015·016 / FR-005 → TS-001·015·016 / FR-006 → TS-008 / FR-007 → TS-005·011 / FR-008 → TS-005·018 / FR-009 → TS-010·011 / FR-010 → TS-001·009 / FR-011 → TS-012·013·014 / FR-012 → TS-006 / FR-013 → TS-009. 빠진 FR 없음.

### 2차 검증 (2026-08-18, spec 1.2.0)

`[TBD]` 마커 **0건**, 실패 항목 **없음**. 전 16항목 통과.

**이번 개정에서 대조한 것**

- 기준 PRD 4.1.0 → 4.2.0 재대조. 프로필에 닿는 변경은 **「비회원 디바이스 세션」 → 「비회원 익명 세션」** 하나이며, 이용자 구분의 근거가 앱이 만든 기기 식별자에서 **인증 제공자가 발급한 익명 계정 식별자**로 바뀌었다. 세션이 기기가 아니라 **앱 설치**에 묶인다는 점도 함께 확정됐다.
- 이 변경은 §4 가정·§2.3 도메인 정의·§3.2 비목표와 어휘(§1 진입/완료 조건, TS-001, FR-007)에만 닿았다. **FR-001~FR-013 · UX-001~UX-006의 판정 내용은 하나도 바뀌지 않았고** 유저 시나리오 3종과 TS-001~TS-017도 그대로다.
- PRD 4.2.0의 홈 카드 변경(저장 경과일을 화면에 표시하지 않음)은 [SCR-003] 범위이며 프로필 스펙과 겹치지 않는다.

**PRD 4.2.0이 새로 연 미확정 3건을 이번 개정에서 닫았다**

- 세션 확보 실패 상태의 화면 도달 → **도달 불가**(스플래시가 차단). 이 스펙은 유효한 세션을 진입 전제로 유지한다
- 재설치로 프로필이 사라졌을 때의 안내 → **알리지 않고** 최초 실행과 동일하게 온보딩 진입
- 서버의 세션 자동 무효화 대응 → **이 스펙 범위 밖**. 익명 세션 스펙이 감지·재발급을 정의한다

앞의 둘은 §4 가정에, 셋째는 §3.2 비목표에 반영하고 §5 「확정된 항목」에 질문·답변을 기록했다.

**FR ↔ 테스트 시나리오 대응 (재확인)**

FR-001 → TS-007 / FR-002 → TS-002·012·013·014·017 / FR-003 → TS-003·004 / FR-004 → TS-001·002·015·016 / FR-005 → TS-001·015·016 / FR-006 → TS-008 / FR-007 → TS-005·011 / FR-008 → TS-005 / FR-009 → TS-010·011 / FR-010 → TS-001·009 / FR-011 → TS-012·013·014 / FR-012 → TS-006 / FR-013 → TS-009. 빠진 FR 없음.

### 1차 검증 (2026-08-18, spec 1.1.0)

`[TBD]` 마커 **0건**, 실패 항목 **없음**. 전 항목 통과. 기준 PRD 3.1.0 → 4.1.0 재대조에서 어긋남 1건(다크모드 비목표 제거)을 반영하고, 1.0.0에서 추정으로 남았던 가정 3건(닉네임 허용 문자 · 아바타 미선택 시 보관 값 · 수정 중 뒤로가기)을 확정했다.

## 비고

- 미완료로 표시된 항목은 `/mino-plan` 실행 전에 spec 수정이 필요하다
