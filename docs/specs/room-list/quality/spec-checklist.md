# Spec 품질 체크리스트: 방 리스트 탭 (Room List Tab)

**목적**: Plan 단계로 넘어가기 전 Spec의 완성도와 품질을 검증한다

**작성일**: 2026-08-24

**대상 스펙**: [spec.md](../spec.md) - 2.1.5

**상태** : PASS

---

## 스펙 품질 (Spec Quality)

- [x] 구현 세부사항(언어, 프레임워크, API)이 없다
- [x] 사용자 가치와 비즈니스 요구에 초점이 맞춰져 있다
- [x] 비기술 이해관계자(기획자, 디자이너 등)가 읽을 수 있게 작성되었다
- [x] 필수 섹션이 모두 채워져 있다

## 요구사항 완전성 (Requirement Completeness)

- [x] `[TBD]` 마커가 남아 있지 않다
- [x] 요구사항이 검증 가능하고 모호하지 않다
- [x] 테스트 시나리오 및 완료조건이 모두 정의되어 있다
- [x] 엣지 케이스가 식별되어 있다
- [x] 핵심 UX 규칙이 측정 가능하다
- [x] 핵심 UX 규칙이 기술 중립적이다 (구현 세부사항 없음)
- [x] 범위가 명확히 한정되어 있다
- [x] 의존성과 가정이 식별되어 있다

## 스펙 완성도 (Spec Completeness)

- [x] 모든 기능 요구사항에 해당하는 명확한 테스트 시나리오가 있다
- [x] 유저 시나리오가 주요 플로우를 포괄한다
- [x] 기능이 성공 기준에 정의된 측정 가능한 결과를 충족한다
- [x] 구현 세부사항이 명세로 새어 들어오지 않았다

## 비고

- 미완료로 표시된 항목은 `/mino-plan` 실행 전에 spec 수정이 필요하다

### 해소된 문제

- **`[TBD]` (EC-005)**: Nudge 재노출 정책은 room-list의 범위가 아니라 [SYS-009] 공동방 생성 유도 spec 소유로 확정되어, §3.2 비목표에 반영하고 §5에 확정 기록했다.
- **FR-011 테스트 시나리오 누락**: TS-013·EC-006 추가로 해소했다.

### v1.2.0 재검증

- 담당자 확인 결과 Nudge는 **공동방 0개인 동안 재진입 시마다 재노출**되는 것으로 확정(세션당 1회 제한 없음). §3.2의 SYS-009 위임 문구를 제거하고 FR-008·EC-005·§5를 확정 값으로 갱신, TS-014를 추가했다. 재검증 결과 16개 항목 모두 유지 통과.

### v1.2.1 (PATCH)

- §5 TBD 기록의 표현을 명확화 — "Nudge 재노출"이 별도 특수 로직이 아니라, "탭 재진입 시 항상 `Peek` default로 시작"하는 일반 동작(FR-001)과 "공동방 0개면 Nudge 표출"하는 조건(FR-008·FR-010)이 겹쳐서 나오는 결과임을 명시. FR-001/FR-008/FR-010 자체는 변경 없음. 재검증 결과 16개 항목 모두 유지 통과.

### v1.2.2 (PATCH)

- §5에서 "Peek 기본값"이라는 표현이 Figma의 `(Default)` 프레임 명칭과 혼동될 수 있어 정정. 담당자 확인 결과 `(Default)`는 `Half` 상태(공동방 0개, `내 장소`만 표시)를 가리키는 Figma 명칭이며, 진입 시 기본 상태인 `Peek`(FR-001)와는 별개임을 §5에 참고 문구로 명시했다. FR-001 등 요구사항 자체는 변경 없음. 재검증 결과 16개 항목 모두 유지 통과.

### v2.0.0 (MAJOR)

- 담당자 재확인 결과 방 리스트 탭의 **진입 기본값이 `Peek`가 아니라 `Half`**로 확정되었다(v1.2.2에서 참고용으로 남겼던 구분이 실제로는 진입 상태 자체를 가리키는 것으로 정정됨). FR-001 요구사항 문장을 재정의하고, 유저 플로우 1의 단계 순서(진입→`Half`, 아래로 드래그→`Peek`)를 뒤집었으며, TS-001을 "진입 시 Half 기본값"으로 교체하고 TS-015(Half→Peek 드래그)를 신규 추가, §4 가정과 §5 TBD 기록의 `Peek` 언급을 `Half`로 정정했다. 요구사항의 실질 의미가 바뀌는 변경이라 MAJOR로 처리. 재검증 결과 16개 항목 모두 통과(FR-001 대응 테스트 시나리오는 TS-001·TS-015로 커버리지 유지).
- plan.md·tasks.md는 아직 생성되지 않아 재실행 대상 없음.

### v2.1.0 (MINOR)

- 기준 PRD가 3.1.0 → 4.1.0으로 개정되며 room-list([SCR-004]) 범위에 영향을 준 두 항목을 반영했다.
  - **FR-001 위치 권한 요청 조건화**: 탭 진입 시 무조건 요청하던 것을, [SCR-003] 홈 탭 `가까운순` 전환이나 [SCR-008] 마이페이지 위치 스위치에서 이미 허용한 이력이 있으면 다시 묻지 않는 것으로 정정(PRD 「지연 권한 요청」 정의 갱신 반영). 유저 플로우 1 1단계 서술과 §4 가정에도 동일하게 반영. TS-016 신규 추가.
  - **[SCR-005] [X] 복귀 시 상태 유지 예외 추가**: 방 상세에서 [X]로 돌아올 때는 진입 기본값(`Half`)이 아니라 `room-detail` spec FR-004의 상태 유지 규칙을 따르는 것으로 EC-007 신규 추가. 이 규칙 자체의 소유는 `room-detail` spec(FR-004)에 두고, room-list §3.2 비목표에 소유 관계를 명시해 중복 정의를 피했다.
  - 기존 FR을 삭제하거나 의미를 뒤집지 않고 조건·예외만 추가하는 변경이라 MINOR로 처리. 재검증 결과 18개 항목 모두 통과(FR-001 대응 테스트 시나리오는 TS-001·TS-015·TS-016으로 커버리지 확장).
- plan.md·tasks.md는 여전히 생성되지 않아 재실행 대상 없음.

### v2.1.1 (PATCH)

- 기준 PRD가 4.1.0 → 4.2.0으로 개정됐으나, 4.2.0의 변경분은 [SCR-003] 홈 카드 덱 카드 구성 정정뿐이라 room-list([SCR-004]) 범위에 영향이 없다. 헤더의 기준 PRD 버전만 갱신하고 본문은 손대지 않았다. 재검증 결과 18개 항목 모두 유지 통과.
- plan.md(1.0.0)·tasks.md는 이미 생성돼 있으나, 이번 변경이 내용에 영향을 주지 않아 재실행 대상 없음.

### v2.1.2 (PATCH)

- MASC 대시보드 검증에서 spec에 Figma 출처가 없다는 지적을 반영해, 요구사항 의미 변경 없이 FR-001~FR-004·EC-003·§2.3 방 리스트 시트 정의에 해당 Figma 노드 링크(피그마 파일 `5P3HE7q8MGc6yAr4rTOSZn`, 노드 9개: 2370-114745, 2661-157242, 2661-157338, 2661-157259, 2392-123138, 2661-158108, 2661-158144, 2661-159182, 2661-158125)를 근거로 추가했다. 재검증 결과 18개 항목 모두 유지 통과.
- plan.md(1.0.0)·tasks.md는 이미 생성돼 있으나, 이번 변경이 내용에 영향을 주지 않아 재실행 대상 없음.

### v2.1.3 (PATCH)

- v2.1.2에서 Figma 링크를 FR 근거 문구 안에만 추가했으나, 템플릿이 정한 위치(각 유저 플로우 제목 바로 아래의 `**Figma**:` 줄)에는 반영되지 않아 MASC 대시보드가 여전히 출처를 비어 있다고 인식했다. 요구사항 의미 변경 없이 유저 플로우 1·2 상단에 `**Figma**:` 인용 줄을 표준 형식으로 추가하고, 플로우 2 하단에 잘못된 라벨(`Figma 근거:`)로 남아 있던 중복 줄을 제거했다. 유저 플로우 3([SYS-001] 생성 폼)·4([SYS-009] Nudge)는 대응하는 Figma 화면이 이 spec 범위 밖이라 `**Figma**:` 줄을 생략했다(템플릿상 조건부 항목). 재검증 결과 18개 항목 모두 유지 통과.
- plan.md(1.0.0)·tasks.md는 이미 생성돼 있으나, 이번 변경이 내용에 영향을 주지 않아 재실행 대상 없음.

### v2.1.4 (PATCH)

- 헤더 `입력` 필드가 최초 작성(2.1.0) 시점의 설명에 고정된 채 이후 개정(2.1.1~2.1.3)의 실제 입력을 반영하지 않아, 이번 개정 라운드의 실제 입력(Figma 노드 URL 9개 재요청)으로 갱신했다. 최초 작성일과 원본 기능 설명은 참고용으로 괄호에 보존. 본문(FR-001~004·유저 플로우 1·2·§2.3 도메인 정의)의 Figma 인용은 v2.1.3에서 이미 표준 위치에 반영되어 있어 변경 없음. 재검증 결과 18개 항목 모두 유지 통과.
- plan.md(1.0.0)·tasks.md는 이미 생성돼 있으나, 이번 변경이 내용에 영향을 주지 않아 재실행 대상 없음.

### v2.1.5 (PATCH)

- 기준 PRD가 4.2.0 → 5.0.0으로 개정되어 재검증했다. PRD diff(commit `c6c7fed`..`8ab1d05`)를 대조한 결과 [SCR-004] 방 리스트 탭 섹션 자체는 이번 개정에서 요구사항 경계가 바뀌지 않았다(변경분은 [SCR-003] 홈 가이드 Flow H, [SYS-002]/[SYS-003] 방 선택 시트 규칙, 익명 세션 전제, 마이페이지 약관 쪽이며 room-list와 직접 연관 없음). 헤더의 기준 PRD 버전만 갱신하고 FR·UX·유저 시나리오 본문은 손대지 않았다.
  - 이번 요청에서 전달된 Figma 노드 URL 8개 중 기존 spec이 참조하던 `2661-158144`(003-2-2 half_공동방 1개)가 누락되어 사용자에게 확인했고, 사용자가 해당 링크를 다시 제공해 유지가 확정되었다(제거 아님). §2.3·FR 근거의 9개 노드 링크 구성은 v2.1.2 기준 그대로 유지.
  - 재검증 결과 18개 항목 모두 유지 통과.
- plan.md(1.0.0)·tasks.md는 이미 생성돼 있으나, 이번 변경이 내용에 영향을 주지 않아 재실행 대상 없음.
