# Spec 품질 체크리스트: 방 상세 (Room Detail)

**목적**: Plan 단계로 넘어가기 전 Spec의 완성도와 품질을 검증한다

**작성일**: 2026-08-25

**대상 스펙**: [spec.md](../spec.md) - 2.1.3

**상태** : PASS

---

## 스펙 품질 (Spec Quality)

- [x] 구현 세부사항(언어, 프레임워크, API)이 없다
- [x] 사용자 가치와 비즈니스 요구에 초점이 맞춰져 있다
- [x] 비기술 이해관계자(기획자, 디자이너 등)가 읽을 수 있게 작성되었다
- [x] 필수 섹션이 모두 채워져 있다

## 요구사항 완전성 (Requirement Completeness)

- [x] `[TBD]` 마커가 남아 있지 않다
- [x] 요구사항이 검증 가능하고 모호하지 않다
- [x] 테스트 시나리오 및 완료조건이 모두 정의되어 있다
- [x] 엣지 케이스가 식별되어 있다
- [x] 핵심 UX 규칙이 측정 가능하다
- [x] 핵심 UX 규칙이 기술 중립적이다 (구현 세부사항 없음)
- [x] 범위가 명확히 한정되어 있다
- [x] 의존성과 가정이 식별되어 있다

## 스펙 완성도 (Spec Completeness)

- [x] 모든 기능 요구사항에 해당하는 명확한 테스트 시나리오가 있다
- [x] 유저 시나리오가 주요 플로우를 포괄한다
- [x] 기능이 성공 기준에 정의된 측정 가능한 결과를 충족한다
- [x] 구현 세부사항이 명세로 새어 들어오지 않았다

## 비고

- 미완료로 표시된 항목은 `/mino-plan` 실행 전에 spec 수정이 필요하다
- 2026-08-16 1차 검증: 14/16 통과, TBD 1건(장소 카드 더보기 "장소 이동" 포함 여부) 발견.
- 2026-08-16 재검증: 사용자 확인 후 TBD 해소(포함하지 않음으로 확정, FR-014·EC-007에 반영) → 16/16 통과.

### v2.0.0 (MAJOR) — 2026-08-18 검증

- 기준 PRD가 3.1.0 → 4.1.0으로 개정되며, [SYS-003] 방 선택 시트의 "이미 저장된 방" 처리 규칙이 PRD 문면상 **확정 → 미확정(TBD)** 으로 되돌아갔다(PRD 4.1.0: [SYS-002]가 링크 분석을 저장 이후로 미루면서 [SYS-003]도 함께 미확정 처리). EC-004를 일시적으로 `[TBD]`로 전환했다.
- Figma 실물(node `2862-175295`, "004-2-2 다른 방에 공유 클릭") 대조 결과, PRD의 TBD는 [SYS-002](분석 지연) 고유의 문제이며 [SYS-003](이미 분석된 장소 복제)에는 적용되지 않음을 확인 — 이미 저장된 방 카드가 Checked+Disabled(투명도 0.43)로 명시적으로 그려져 있어 기존 확정값(비활성 처리)을 그대로 유지. EC-004를 재확정하고 §5에 근거를 기록했다.
- 같은 Figma 대조 과정에서 FR-006(카테고리 칩)이 오래된 Figma 주석("저장 값 기반 동적 생성")을 그대로 인용해 PRD의 확정 사항("전체/카페/음식점 3종 고정", 비목표에도 동적 생성 제외 명시)과 어긋나 있던 기존 버그를 발견 — PRD 4.1.0 개정과 무관하게 v1.0.0부터 있던 오류. FR-006·§4 가정을 3종 고정으로 정정하고 최신 목업(node `3510-125317`·`3222-87768`)으로 재확인했다.
- 확정된 요구사항의 의미가 두 군데(EC-004, FR-006)에서 바뀌는 변경이라 MAJOR로 처리. 재검증 결과 16/16 통과, `[TBD]` 잔여 0건.

### v2.0.1 (PATCH)

- Figma node `2542-125408`(정렬 드롭다운 펼친 상태)와 `2542-125426`(칩 닫힌 상태) 대조 결과, 정렬 드롭다운 5종 항목·카테고리 칩 3종 모두 spec과 일치했으나 정렬 드롭다운 표시 순서만 어긋나 있었다("꾹 Pick/전체/…" → PRD·Figma 원래 순서인 "전체/꾹 Pick/…"로 정정). FR-005·유저 플로우 2 1단계 표현만 바뀌고 요구사항 의미는 그대로라 PATCH로 처리. 재검증 결과 16/16 통과 유지.

### v2.0.2 (PATCH)

- 디자이너가 §5의 EC-004 TBD 해소 문구(v2.0.0에서 작성)를 읽고 헷갈려함 — "[SYS-002] 타이밍 문제" 같은 개발 용어 위주 서술이라 비개발자가 이해하기 어려웠다. 같은 결론(비활성 처리 유지)은 그대로 두고, "왜 이 화면은 영향이 없는지"를 기능 이름 대신 평이한 말(새로 공유받은 링크 저장 vs 이미 저장된 장소를 다른 방에 복사)로 다시 썼다. 결론·근거 자체는 바뀌지 않아 PATCH로 처리. 재검증 결과 16/16 통과 유지.

### v2.1.0 (MINOR) — 2026-08-25 검증

- 기준 PRD가 4.1.0 → 6.0.0으로 개정됨에 따라 재검토했다. PRD 커밋 이력(`git log`로 4.2.0/5.0.0/5.1.0/6.0.0 diff 대조)을 확인한 결과, 6.0.0 개정의 핵심은 [SCR-003] 홈 탭·[SCR-006] 장소 상세 쪽이며 [SCR-005] 방 상세 본문 자체의 실질 변경은 크지 않았다.
- PRD 5.0.0에서 방 편집 버튼·완료 스낵바 문구가 정확화됨: "[편집]"→"[방 편집]", "[편집 완료]"→"[방 편집 완료]", 완료 스낵바 문구 `방 편집이 완료되었어요` 확정. FR-012·유저 플로우 4·TS-012에 반영.
- Figma 실물 대조(node `2542-125341`)로 장소 수 인디케이터의 표기 형식(`{N}개`, 999개 초과 시 `999+개`)을 새로 확인해 FR-001·유저 플로우 1에 추가.
- Figma 실물 대조(node `2542-125613`, 사용자가 지목한 값)로 [친구 +] 초대 바텀시트(424dp) 내 참여자 목록의 스크롤 영역이 288dp(고정값)임을 확인해 FR-011·유저 플로우 4에 추가.
- 정렬 드롭다운 표시 순서를 재확인하는 과정에서 Figma 두 노드(`2542-125408`과 `2542-125333`)가 서로 다른 순서("전체" 우선 vs "꾹 Pick" 우선)를 보여주는 것을 발견했다. `2542-125408`은 PRD 본문 순서·v2.0.1 PATCH의 기존 결론과 일치하고, `2542-125333`은 다른 화면(리스트형 전체 화면 목업)에 박힌 오버라이드로 최신성이 불확실해 `2542-125408`·PRD 순서를 그대로 유지했다(FR-005 변경 없음). 두 프레임 간 불일치 자체는 디자인 쪽에 확인이 필요한 사항이나, spec의 요구사항 의미를 바꾸지 않으므로 이번 개정 범위에는 포함하지 않았다.
- 새 사실 3건(스낵바 문구, 장소 수 표기, 스크롤 영역)을 기존 FR 위에 추가하고 기존 확정 요구사항의 경계는 바꾸지 않아 MINOR로 처리. 재검증 결과 16/16 통과, `[TBD]` 잔여 0건.

### v2.1.1 (PATCH)

- v2.1.0에서 남겨뒀던 정렬 드롭다운 순서 불일치(node `2542-125408` "전체 먼저" vs node `2542-125333` "꾹 Pick 먼저")를 사용자가 실기기 스크린샷과 함께 디자이너에게 직접 확인 요청했다. 디자이너 답변: **`꾹 Pick`이 먼저**(`꾹 Pick`/`전체`/`최신순`/`거리순`/`코멘트순`) — `2542-125408`이 낡은 시안이었음이 확정됨.
- FR-005·유저 플로우 2 1단계의 순서 표기를 `전체/꾹 Pick/…`에서 `꾹 Pick/전체/…`로 정정했다. 요구사항의 의미(정렬 드롭다운을 제공하고 선택에 따라 재정렬한다)는 바뀌지 않고 표시 순서 문구만 바뀌어 PATCH로 처리. 재검증 결과 16/16 통과 유지.

### v2.1.2 (PATCH)

- 사용자가 "출처(Figma)" 목록에 링크가 너무 적다고 지적 — spec 안에 인용된 Figma 노드 13곳 중 상단 "**Figma**:" 캡션 3개만 실제 하이퍼링크였고, FR-001·FR-005·FR-006·FR-009·FR-011·FR-014, §4 가정, §5 TBD 안의 나머지 노드는 `node xxxx` 텍스트로만 적혀 있어 링크만 추출하는 도구에서 누락됐다.
- 모든 노드 인용을 `[node {id}](https://www.figma.com/design/5P3HE7q8MGc6yAr4rTOSZn/MU_디자인?node-id={id}&m=dev)` 형태의 클릭 가능한 링크로 통일했다. 요구사항 내용·근거 자체는 바뀌지 않은 순수 서식 정리라 PATCH로 처리. 재검증 결과 16/16 통과 유지.

### v2.1.3 (PATCH)

- MASC 대시보드의 "출처(Figma)" 목록이 여전히 3개만 보인다는 사용자 지적 — 대시보드는 문장 중간의 인라인 링크가 아니라 각 유저 플로우 상단의 `**Figma**:` 캡션 줄만 출처로 추출하고 있었다. 유저 플로우 1·3에만 그 캡션 줄이 있었고 유저 플로우 2·4에는 아예 없어서, v2.1.2에서 본문에 링크를 추가했어도 대시보드에는 안 잡혔다.
- 유저 플로우 2("정렬 드롭다운 펼친 상태" `2542-125333` · "카테고리 칩 3종 고정" `3510-125317`)와 유저 플로우 4("친구 초대 참여자 목록" `2542-125613`)에 `**Figma**:` 캡션 줄을 새로 추가했다. 유저 플로우 1·3의 기존 캡션 줄에도 관련 노드(`2542-125341`, `3222-87768`)를 추가했다.
- 같은 과정에서 유저 플로우 2 2단계가 v2.0.0에서 FR-006을 3종 고정으로 정정할 때 함께 안 고쳐진 채 "전체 + 저장된 장소 기반 동적 생성"으로 남아 있던 걸 발견 — FR-006과 일치하도록 "`전체`/`카페`/`음식점` 3종 고정"으로 정정했다. 캡션 줄 추가와 함께 발견된 표현 버그 수정이라 PATCH로 처리. 재검증 결과 16/16 통과 유지.
