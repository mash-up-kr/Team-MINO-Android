package team.mino.core.designsystem.foundation.shape.token

import androidx.compose.foundation.shape.RoundedCornerShape

internal object ShapeTokens {
    val Small = RoundedCornerShape(AtomicShapeToken.CornerSmall)
    val Medium = RoundedCornerShape(AtomicShapeToken.CornerMedium)
    val Large = RoundedCornerShape(AtomicShapeToken.CornerLarge)
}
